$ docker images

1. We just downloaded the code of an application. What is the base image used in the Dockerfile?
Inspect the Dockerfile in the webapp-color directory.
A:
You can either open the file using vi /root/webapp-color/Dockerfile (or using commands such as cat/more/less/vim e.t.c) and 
look for the FROM instruction or search for it directly using grep -i FROM /root/webapp-color/Dockerfile.

2. Build a docker image using the Dockerfile and name it webapp-color. No tag to be specified.
A: 
Move to the directory first by using the cd command and verify the path of the working directory from pwd command :-
$ cd /root/webapp-color/
$ pwd
/root/webapp-color
Now, run the docker build command within that directory :-
$ docker build -t webapp-color . 
NOTE: At the end of the command, we used the "." (dot) symbol which indicates for the current directory, so you need to run this command from within the directory that has the Dockerfile.

3. Run an instance of the image webapp-color and publish port 8080 on the container to 8282 on the host.
A:
docker run -p 8282:8080 webapp-color

11. What is the base Operating System used by the python:3.6 image?
If required, run an instance of the image to figure it out.
A:
Run 'docker run python:3.6 cat /etc/*release*' command.              
note: /etc/*release* gives information of your operating system
in this case, if run 'cat /etc/*release*' only, it shows your computer's operating system information

5. Build a new smaller docker image by modifying the same Dockerfile and name it webapp-color and tag it lite.
Hint: Find a smaller base image for python:3.6. Make sure the final image is less than 150MB.
A: 
In the webapp-color directory, run the ls -l command to list the Dockerfile and other files.
And modify Dockerfile to use python:3.6-alpine image and then build using docker build -t webapp-color:lite .


the purpose of sleep: keeps the container running


docker history /root/<image name>    //look up the size of each layer


pod-definition file overwrites docker file: pod file is for container, docker file is for image, so when container is running on image, the configuration of pod will overwrite that of image
in docker file it has: entrypoint and CMD
in pod definition file it has: command and args under spec.containers
command overwrites entrypoint, args overwrites CMD






