when an application interacts with kube-api, it needs service account to authenticate
$ k create serviceaccount <serviceaccount-name>    //when a service account is created, a token is created as well
the token is stored as a secret object, to view the token:
k describe secret <token-name>

if the application is hosted on the kubernetes itself, the process of exporting the token and config the third party application to use it can be done by mounting 
the token as a volumn inside the pod hosting the application.


any change of service account in a pod definition file would automatically trigger a new roll out (deployment) of the application

k describe pod <pod-name>

k exec -it <pod-name> ls /var/run/secrets/kubernetes.io/serviceaccount    //look up the secrets mounted to the pod inlcuding token

k exec -it <pod-name> cat /var/run/secrets/kubernetes.io/serviceaccount/token


decode token website: jwt.io


generate a token for a service account:
kubectl create token <serviceaccount-name>
