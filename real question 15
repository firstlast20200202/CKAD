k describe ns haddock

//edit the container part in the deployment file: spec.template.spec.container
k -n haddock edit deployment nosql

//verify 
k -n haddock describe deployment nosql

note: if edit the deployment directly, it should work, but not write in the yaml file, 
the reason is, the yaml file is an old one already, if i want a new one, should -o yaml 
