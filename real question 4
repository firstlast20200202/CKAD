k -n quetzal get all 

k -n quetzal get pod broker-deployment-554c49db77-pk27k -o yaml > oo.yaml

vi oo.yaml

//add this part under container:
SecurityContext: 
  runAsUser: 30000
  allowPrivilegeEscalation: false
  capabilities: 
    Add: ["NET_BIND_SERVICE"]

k replace --force -f oo.yaml


note：
securityContexts可以添加在两处：deployment的pod级别和deployment的container里 添加在pod级别的话就是作用于所有的containers 答题一定要看准让添加在哪里 这里是添加在单一容器里
