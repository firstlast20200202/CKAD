kubectl run hello-minikube    //deploy an application on a cluster

kubectl cluster-info

kubectl get nodes


crictl   //only used to debug

nerdctl   //for general purpose

pod is infrasturcture of running containers
image is dependencies and libraries,image is the thing that container runs, cotaner is a running instance of an image

lab:
Create a new pod using the nginx image.
kubectl run nginx --image=nginx

